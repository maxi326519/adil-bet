import '../actions/types';

const initialState = {
    // Agregrar las variables necesarias
}

export const Reducer = (state = initialState, action)=>{
    switch(action.type){
        // Agregar cada caso
    }
}